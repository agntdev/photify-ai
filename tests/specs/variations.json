[
  {
    "name": "Variation on missing asset shows not found",
    "steps": [
      {
        "send": { "callback": "var:start:nonexistent" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "That portrait wasn't found — it may have expired."
            }
          }
        ]
      }
    ]
  }
]
